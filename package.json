{
  "name": "apple-google-pay-express-checkout-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Live customer-call demo: Apple Pay / Google Pay via the Express Checkout Element, showing exactly what data comes back sync (client confirm) vs async (webhook).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^17.7.0"
  }
}
